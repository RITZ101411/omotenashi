import { View, Text } from "react-native";

// カメラ画面（写真撮影）
export default function CameraScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-black">
      <Text className="text-white">カメラ画面</Text>
    </View>
  );
}
